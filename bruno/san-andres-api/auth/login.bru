meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "loreto.cg@gmail.com",
    "password": "Lo.89104665"
  }
}

script:post-response {
  if (res.status === 200 || res.status === 201) {
    bru.setEnvVar("access_token", res.body.access_token);
  }
}
