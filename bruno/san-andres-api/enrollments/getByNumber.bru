meta {
  name: getByNumber
  type: http
  seq: 4
}

get {
  url: {{BASE_URL}}/enrollments/number/:enrollmentNumber
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:path {
  enrollmentNumber: <numero_de_matricula>
}

settings {
  encodeUrl: true
  timeout: 0
}
