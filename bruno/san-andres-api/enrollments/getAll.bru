meta {
  name: getAll
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/enrollments
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
